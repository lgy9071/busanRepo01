<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.theater.dao.IPerformanceDao">

	<select id="getList" resultType="performanceDto">
   		 SELECT * FROM tperformance ORDER BY pid DESC
	</select>
	
	<select id="getAll" resultType="com.example.theater.dto.performanceDto">
    	SELECT * FROM tperformance ORDER BY pid DESC
	</select>
	
</mapper>